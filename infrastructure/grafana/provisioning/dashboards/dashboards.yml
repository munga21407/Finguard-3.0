apiVersion: 1

providers:
  - name: finguard
    orgId: 1
    folder: FinGuard
    folderUid: finguard
    type: file
    disableDeletion: true
    editable: true
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
